const reveals=document.querySelectorAll(".reveal");const observer=new IntersectionObserver(e=>{e.forEach(t=>{if(t.isIntersecting)t.target.classList.add("visible")})},{threshold:.12});reveals.forEach(e=>observer.observe(e));const counters=document.querySelectorAll("[data-count]");const counterObserver=new IntersectionObserver(e=>{e.forEach(t=>{if(!t.isIntersecting)return;const n=t.target,o=Number(n.dataset.count);let r=0;const s=Math.max(1,Math.ceil(o/48)),i=setInterval(()=>{r+=s;if(r>=o){r=o;clearInterval(i)}n.textContent=r},24);counterObserver.unobserve(n)})});counters.forEach(e=>counterObserver.observe(e));const menu=document.querySelector(".menu-button"),nav=document.querySelector(".nav");menu?.addEventListener("click",()=>nav.classList.toggle("open"));nav?.querySelectorAll("a").forEach(e=>e.addEventListener("click",()=>nav.classList.remove("open")));const glow=document.querySelector(".cursor-glow");window.addEventListener("pointermove",e=>{if(!glow)return;glow.style.left=`${e.clientX}px`;glow.style.top=`${e.clientY}px`});